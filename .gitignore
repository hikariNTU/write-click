node_modules/
dist/
dist-dev/
dist.zip
.DS_Store
*.log

# Captures are regenerated by `npm run shots`, never committed. The listing
# images they compose into are regenerated by `npm run shots:slides` and are not
# committed either: they are derived from a capture set that is not in the
# repository, so a committed PNG could only ever be older than its source.
shots/out/
shots/submit/
shots/.run/
